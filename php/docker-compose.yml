version: '3'
services:
 # Php-fpm configuration
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .././app/html:/var/www/html
      - ./config/php.ini:/usr/local/etc/php/php.ini
    networks:
      - mysql
      - php
      - nginx_static

networks:
  mysql:
    name: mysql
  php:
    name: php
  nginx_static:
    name: nginx_static
    ipam:
      driver: default
      config:
        - subnet: "155.220.1.0/16"
